DATABASE_URL="file:./dev.db"
JWT_SECRET="cambia-este-secreto-en-produccion"
PORT=3001

# --- Notificaciones por email (opcional) ---
# Si están vacíos, las notificaciones solo se guardan en BD (sin correo).
# Ejemplo con Gmail (requiere "App Password" de Google, no la contraseña normal):
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=tu.cuenta@gmail.com
#   SMTP_PASS=xxxx xxxx xxxx xxxx
#   SMTP_FROM="Centro Cultural Lucy Tejada <tu.cuenta@gmail.com>"
#
# Ejemplo con SendGrid:
#   SMTP_HOST=smtp.sendgrid.net
#   SMTP_PORT=587
#   SMTP_USER=apikey
#   SMTP_PASS=SG.xxxxxxxxxxxxx
#   SMTP_FROM="Centro Cultural Lucy Tejada <noreply@tudominio.com>"
#
# Ejemplo con Mailtrap (sandbox de pruebas):
#   SMTP_HOST=sandbox.smtp.mailtrap.io
#   SMTP_PORT=2525
#   SMTP_USER=xxxxxxxx
#   SMTP_PASS=xxxxxxxx
#   SMTP_FROM="Lucy Tejada <noreply@lucytejada.test>"
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
